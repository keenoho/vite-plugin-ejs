# vite-plugin-ejs

## usage
vite.config.js
```
import ejsPlugin from './ejs-plugin';

export default {
    ...other config

    plugins: [
      ejsPlugin(),
      ...other plugin
    ],

    ...other config
}
```
